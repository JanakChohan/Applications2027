{
  "name": "aon-scales-trainer",
  "version": "1.0.0",
  "description": "Local practice app for Aon/cut-e scales numerical (True/False/Cannot Say) assessments. Legitimate preparation only.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "audit": "node scripts/audit.js"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
